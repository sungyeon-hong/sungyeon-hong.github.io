
.event-listing-card {
  display: flex;
  flex-direction: column;
  min-width: 0;
  overflow: hidden;
  background: var(--engagement-bg, #ffffff);
  border: 1px solid var(--engagement-line, #d7e1e4);
  border-radius: 18px;
  box-shadow: 0 8px 22px rgba(31, 41, 51, 0.045);
  transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease;
}

.event-listing-card:hover {
  transform: translateY(-3px);
  border-color: rgba(66, 107, 115, 0.42);
  box-shadow: 0 13px 30px rgba(31, 41, 51, 0.085);
}

.event-listing-image-link {
  display: block;
  overflow: hidden;
  border-bottom: 1px solid var(--engagement-line, #d7e1e4);
  line-height: 0;
}

.event-listing-image {
  display: block;
  width: 100%;
  aspect-ratio: 16 / 9;
  object-fit: cover;
  transition: transform 0.2s ease;
}

.event-listing-card:hover .event-listing-image {
  transform: scale(1.015);
}

.event-listing-body {
  display: flex;
  flex-direction: column;
  flex: 1;
  padding: 1.15rem;
}

.event-listing-date-row {
  display: flex;
  align-items: baseline;
  gap: 0.55rem;
  margin-bottom: 0.65rem;
}

.event-meta-label,
.event-page-meta-label,
.event-resource-type {
  text-transform: uppercase;
  letter-spacing: 0.12em;
  font-size: 0.69rem;
  font-weight: 750;
  color: var(--engagement-accent, #426b73);
}

.event-listing-date {
  color: var(--engagement-ink, #1f2933);
  font-size: 0.84rem;
  font-weight: 650;
}

.event-listing-title {
  margin: 0 0 0.65rem;
  font-size: 1.08rem;
  line-height: 1.32;
  letter-spacing: -0.015em;
}

.event-listing-title a,
.event-listing-title a:visited {
  color: var(--engagement-ink, #1f2933);
  text-decoration: none;
}

.event-listing-title a:hover {
  color: var(--engagement-accent, #426b73);
}

.event-listing-description {
  margin: 0 0 1rem;
  color: var(--engagement-muted, #5f6f7a);
  font-size: 0.94rem;
  line-height: 1.6;
}
